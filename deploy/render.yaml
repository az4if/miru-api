services:
  - type: web
    name: miru-api
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: API_KEY
        sync: false
      - key: RATE_LIMIT_RPM
        value: "120"
      - key: LOG_LEVEL
        value: info
